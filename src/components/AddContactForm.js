

const AddContactForm = () => {
  
  const generateId = () => Math.round(Math.random() * 100000000);
  
  return (
    <p>form</p>
  )
  //Full Name
  //Email Address
  //Phone Number
  //Image URL
  //Add Contact Button
}

export default AddContactForm;